version: '3.8'

services:
  flask-app:
    build: .
    container_name: flask-blog-demo
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      - SECRET_KEY=dev-secret-key-change-in-production
    stdin_open: true
    tty: true
    restart: unless-stopped
