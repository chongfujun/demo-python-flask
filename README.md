# Flask Blog Demo

A simple Flask blog system with SQLAlchemy.

## Setup

### Docker
```bash
docker-compose up --build
```

### Local
```bash
pip install -r requirements.txt
python app.py
```

Visit http://localhost:5000

## CodeQL Results

See `ghas-results.sarif` for GitHub Advanced Security scan results (21 alerts).